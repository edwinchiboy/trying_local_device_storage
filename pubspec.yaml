name: testing_storing_device
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_staggered_grid_view: ^0.6.1
  intl: ^0.17.0
  intl_utils: ^2.6.1
  path_provider: ^2.0.9
  shared_preferences: ^2.0.13
  sqflite: ^2.0.2

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - lib/assets/images/product-placeholder.png
  